# Never commit real credentials
backend/.env
.env
.env.local

# Python
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/

# Node (if the frontend is ever run as a standalone Vite/CRA app)
node_modules/
frontend/dist/
frontend/build/

# OS
.DS_Store

# Local SDD scratch workspace (task briefs/reports) -- never a deliverable
.superpowers/
